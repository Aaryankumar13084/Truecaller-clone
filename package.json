{
  "name": "truecaller-clone",
  "version": "1.0.0",
  "description": "A Truecaller clone built with React.js (frontend) and Node.js (backend).",
  "scripts": {
       "start": "cd backend && npm start",
       "build": "cd frontend && npm run build",
       "install-all": "npm install --prefix backend && npm install --prefix frontend",
       "dev": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\""
     }, 
  "keywords": [
    "truecaller",
    "clone",
    "react",
    "node",
    "mongodb"
  ],
  "author": "Your Name",
  "license": "MIT",
 "dependencies": {
       "react": "^17.0.2",
       "react-dom": "^17.0.2",
       "react-router-dom": "^5.2.0",
       "axios": "^0.21.1",
       "react-scripts": "4.0.3"
     }, 
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
